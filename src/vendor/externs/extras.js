// externs that we need and aren't in packages yet
//

var vec3 = {};
vec3.angle = function(a, b) {};


var vis = {};

vis.DataSet = function() {};

vis.Timeline = function() {};
vis.Timeline.prototype.addCustomTime = function(id) {};
vis.Timeline.prototype.on = function(event, f) {};
vis.Timeline.prototype.setCustomTime = function(time, id) {};